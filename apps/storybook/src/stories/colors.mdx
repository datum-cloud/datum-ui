import { Meta, ColorPalette, ColorItem } from '@storybook/blocks'
import themeConfig from '../../tailwind.config.ts'

# Color Palette

<Meta title="Intro/Styling & Theming/Colors" />
<ColorPalette>
  {Object.entries(themeConfig?.presets[0].theme?.extend?.colors || {}).map(
    ([colorName, colorValue]) => (
      <ColorItem key={colorName} title={colorName} colors={colorValue} />
    ),
  )}
</ColorPalette>
