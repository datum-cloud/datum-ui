---
'@datum-cloud/datum-ui': minor
---

Add an optional `sidebarHeader` slot to `AssistantWorkspace`, forwarded to the history panel and rendered above the search input. Lets a host label the scope its saved chats belong to — cloud-portal shows the current project. Purely additive; hosts that omit it render exactly as before.
