---
"@datum-cloud/datum-ui": patch
---

Bump `embla-carousel-react` from `^8` to `^8.6.0` and `input-otp` from `^1` to `^1.4.2` (renovate-flagged within-major range tightening; no behavior change for consumers already on the prior ranges).
