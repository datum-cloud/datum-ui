---
'@datum-cloud/datum-ui': patch
---

Picker triggers (`DatePicker`, `TimeRangePicker`, etc.) now use `rounded-lg`, matching `Input`, `Select`, and `Autocomplete`. Previously they inherited the shadcn Button's `rounded-md`.
